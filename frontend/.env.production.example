# Copy to .env.production before `npm run build` for Pages deploy.
# In dev these can be left unset — Vite's proxy in vite.config.ts routes /api and /ws to localhost:8787.
VITE_API_BASE=https://ai-dm-worker.YOUR-SUBDOMAIN.workers.dev
VITE_WS_URL=wss://ai-dm-worker.YOUR-SUBDOMAIN.workers.dev
